import { base } from '../base';

export const card = {
  background: base.palette.white.normal,

  border: {
    style: 'solid',
    width: '1px',
  },

  colors: {
    border: base.palette.cloud.normal,
  },
};
